// Zod schemas compartidos entre web y mobile.
// Se usan para validar inputs en formularios (web) y sync payload (mobile).

export { z } from 'zod'

// Schemas de validación por módulo se agregan aquí conforme se implementan.
// Ejemplo: export * from './vehicles'
